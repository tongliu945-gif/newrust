# 定制版 RustDesk Android 构建指南

## 🚀 使用 GitHub Actions 自动构建（推荐）

### 第一步：推送到 GitHub

1. **在 GitHub 上创建新仓库**
   - 访问 https://github.com/new
   - 仓库名称：例如 `rustdesk-custom`
   - 选择 Private（私有）或 Public（公开）
   - **不要**初始化 README、.gitignore 或 license

2. **推送代码到 GitHub**
   ```bash
   cd /Users/a58/Desktop/rustdesk-master/rustdesk-master
   
   # 添加远程仓库（替换为你的 GitHub 用户名和仓库名）
   git remote add origin https://github.com/你的用户名/rustdesk-custom.git
   
   # 推送代码
   git branch -M main
   git push -u origin main
   ```

### 第二步：触发构建

#### 方法 1：手动触发（推荐）

1. 在 GitHub 上打开你的仓库
2. 点击 **Actions** 标签页
3. 选择左侧的 **"Build Custom Android APK"** 工作流
4. 点击右侧的 **"Run workflow"** 按钮
5. 确保 "Upload Artifact" 已勾选
6. 点击 **"Run workflow"** 开始构建

#### 方法 2：自动触发

每次推送代码到 main 分支时会自动构建：

```bash
git add .
git commit -m "更新代码"
git push
```

### 第三步：下载 APK

构建完成后（约 15-20 分钟）：

1. 进入 Actions 页面
2. 点击最新的构建任务
3. 滚动到底部的 **"Artifacts"** 部分
4. 下载 `rustdesk-custom-android-apk`

---

## 📋 本地构建（备选方案）

如果你需要在本地构建：

### 使用 Docker（已配置）

```bash
cd /Users/a58/Desktop/rustdesk-master/rustdesk-master
./build-android-docker.sh
```

**注意：** 本地构建需要稳定的网络连接，首次构建可能需要 2-3 小时。

---

## 🔧 常见问题

### Q: GitHub Actions 构建失败怎么办？

A: 查看构建日志，常见原因：
- Rust 编译错误：检查 Rust 代码语法
- Flutter 依赖问题：检查 `flutter/pubspec.yaml`
- NDK 版本问题：workflow 使用 NDK 25.1.8937393

### Q: 如何修改版本号？

A: 编辑 `flutter/pubspec.yaml` 中的 `version` 字段：
```yaml
version: 1.2.3+456
```

### Q: 如何签名 APK？

A: 在 GitHub 仓库设置中添加以下 Secrets：
- `ANDROID_SIGNING_KEY`: Base64 编码的 keystore 文件
- `ANDROID_ALIAS`: Key alias
- `ANDROID_KEY_STORE_PASSWORD`: Keystore 密码
- `ANDROID_KEY_PASSWORD`: Key 密码

然后修改 workflow，添加签名步骤。

---

## 📝 注意事项

1. **首次构建时间较长**：GitHub Actions 需要下载所有依赖
2. **后续构建会更快**：GitHub 会缓存依赖
3. **分支保护**：建议在开发分支测试，确认无误后再合并到 main
4. **私有仓库**：如果是商业项目，请使用私有仓库
5. **存储空间**：GitHub 免费账户有存储和构建时间限制

---

## 🎯 下一步

- [ ] 推送代码到 GitHub
- [ ] 触发第一次构建
- [ ] 测试生成的 APK
- [ ] 配置自动发布（可选）
- [ ] 添加代码签名（可选）

